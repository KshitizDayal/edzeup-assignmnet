import 'package:flutter/material.dart';

import 'color_constants.dart';

const textStyleheading =
    TextStyle(fontSize: 24, color: blackColor, fontWeight: FontWeight.bold);

const textStyle = TextStyle(fontSize: 14, color: blackColor);

const textStyleWhite = TextStyle(fontSize: 14, color: whiteColor);
